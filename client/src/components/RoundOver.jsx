import React from 'react'

// i am not using this component yet but my hope is to build a round summary
// that will be seen at the end of the round before moving on the the next round.
// or at the very least it should display something like "good work! you've finished the round!"
function RoundOver({currentUser}) {
  return (
    <div>RoundOver</div>
  )
}

export default RoundOver